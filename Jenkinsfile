pipeline {
    agent any
    stages {
        stage('build') {
            steps {
                sh 'cat "Hello Jenkins!"'
                sh 'ls -alh'
                sh 'python --version'
            }
        }
    }
}
