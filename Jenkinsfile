pipeline {
    agent any
    stages {
        stage('build') {
            steps {
                sh 'echo "Hello Jenkins!"'
                sh 'ls -alh'
                sh 'python --version'
            }
        }
    }
}
